VERSION: 1.3.1
DETAILS:

new: Duplicate Apps on Bar — add the same app multiple times, each with its own independent menu configuration.
new: "Duplicate" context menu action — right-click any bar item and choose "Duplicate" to clone it with its menu configuration.
new: Native App Icon Picker — browse and pick icons directly from installed applications on your Mac.
bug fix: Fixed icon sizing inconsistencies when using native app icons.
bug fix: Widgets now correctly allow selecting Finder aliases and folders as targets (previously aliases were resolved, breaking network share paths).
